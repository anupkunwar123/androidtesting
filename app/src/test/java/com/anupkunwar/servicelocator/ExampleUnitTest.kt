package com.anupkunwar.servicelocator

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }

    //unit test view model
    //when progressing is loading should be true
    //when either error or success isLoading set to be false

    //on main activity when loading progress bar visible
    //on success name are displayed right

}
